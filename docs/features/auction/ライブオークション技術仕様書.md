# ライブオークション技術仕様書（エンジニア向け）

**文書番号**: AUC-TECH-2026-001
**版数**: 1.0
**最終更新**: 2026-04-30
**対象環境**: 本番（AWS EC2 AL2023 / `/var/www/auction` / Supervisor + Redis 3キュー）

---

## 0. このドキュメントの位置づけ

ライブオークション（フロアスタート〜成約／流札〜次商品〜オークション終了）に関わる
全コード経路・データフロー・キャッシュ・キュー・WebSocket イベントを **コード行番号付き** でまとめた
内部仕様書。改修時の影響範囲特定とインシデント時の即時参照を目的とする。

非エンジニアは [ライブオークション運用ガイド.md](./ライブオークション運用ガイド.md) を参照。
リスク・ボトルネック分析は [300名負荷リスク評価とレビュー指摘.md](./300名負荷リスク評価とレビュー指摘.md) を参照。

---

## 1. 全体アーキテクチャ

### 1.1 構成要素

```
┌─────────────────────────────────────────────────────────────────┐
│  Browser (React + TanStack Query + Laravel Echo)                │
│  - participant: AuctionLive.tsx / AuctionItems.tsx              │
│  - admin:       LiveControl.tsx                                 │
└─────┬──────────────────────────────────────┬────────────────────┘
      │ HTTPS REST                           │ WSS (Reverb)
      │                                      │
┌─────▼──────────────┐               ┌───────▼─────────┐
│  Laravel HTTP      │               │  Laravel Reverb │
│  (nginx + php-fpm) │               │  (port 6001)    │
│  api.php routes    │               │  supervisor     │
└─────┬──────────────┘               └───────▲─────────┘
      │                                      │ broadcast()
      │  ┌───────────────────────────────────┤
      │  │                                   │
┌─────▼──▼─────────┐  ┌────────────┐  ┌──────▼────────────────────┐
│  MySQL (RDS)     │  │  Redis     │  │  Queue Workers            │
│  items / lanes   │  │  (Valkey)  │  │  (Supervisor 管理)        │
│  bid_participants│  │  cache /   │  │  - queue-default          │
│  bid_events      │  │  lock /    │  │  - queue-countdown ★      │
│  bid_limit_prices│  │  queue     │  │  - queue-notifications    │
└──────────────────┘  └────────────┘  └───────────────────────────┘
```

★ `queue-countdown` には `ProcessAuctionCountdownJob` だけが流れる。1オークション = 1ジョブ。

### 1.2 1リクエストの処理経路（入札ON）

```
[端末] tap "ON"
   │
   ▼ POST /api/participant/bids  (auth:sanctum, check.subscription:bid)
[BidController::toggle]   src/app/Http/Controllers/Participant/BidController.php:27
   │
   ▼ DI 注入された JoinBidAction
[JoinBidAction::execute]  src/app/Actions/Bid/JoinBidAction.php:25
   │
   ├─ 1. item.status / auction.status 確認
   ├─ 2. countdown:lane:{id} cache から phase 判定 (pre_bid/freeze なら拒否)
   ├─ 3. bid_limit_prices で上限価格チェック
   ├─ 4. Cache::lock("bid_inflight:item:{id}", 5)  ← ★ 応用層直列化
   ├─ 5. DB::transaction([items行ロック → phase再判定 → 価格再検証 →
   │                       bid_participants.participate → bid_events.recordJoin →
   │                       activeCount = SELECT COUNT(*)])
   ├─ 6. countdown cache に last_bidder_user_id 書き込み
   ├─ 7. broadcast(BidderUpdated::joined)  ← Reverb へ
   ├─ 8. activeCount >= 2 なら handleImmediatePriceIncrement
   └─ 9. finally: Redis lock release
```

---

## 2. データモデル

### 2.1 主要テーブル

| テーブル | 役割 | 重要カラム |
|---|---|---|
| `auctions` | オークション本体 | `status` (draft/scheduled/preparing/live/finished/cancelled), `event_date`, `lane_count`, `auction_settings`(JSON) |
| `lanes` | 同時並行レーン | `lane_number`, `current_item_id`, `status` (waiting/active/paused/finished) |
| `items` | 出品商品 | `auction_id`, `lane_id`(任意), `status` (draft/registered/live/sold/unsold/cancelled), `start_price`, `current_price`, `bid_increment` |
| `lane_items` | レーンと商品の紐付け（中間） | `lane_id`, `item_id`, `sequence_order` |
| `bid_participants` | 入札ON/OFF状態 | UNIQUE(`item_id`,`user_id`), `is_active`, `activated_at`, `deactivated_at` |
| `bid_events` | 入札・離脱・自動離脱の履歴（監査用） | `item_id`, `user_id`, `event_type` (`join`/`leave`/`auto_left`), `price`, `created_at` |
| `bid_limit_prices` | 指値（上限価格） | UNIQUE(`item_id`,`user_id`), `limit_price`, `is_triggered`, `triggered_at` |
| `price_events` | 価格変動の履歴 | `item_id`, `old_price`, `new_price`, `event_type` |
| `won_items` | 落札確定 | `item_id`, `user_id`, `final_price`, `won_at` |
| `favorites` | お気に入り | UNIQUE(`user_id`,`item_id`) |

### 2.2 重要インデックス

- `items` : `(auction_id, status, item_number)` … ライブ中商品リスト取得
- `bid_participants` : `(item_id, is_active)` … active カウント高速化
- `bid_limit_prices` : UNIQUE `(item_id, user_id)` … 指値再設定の上書き

### 2.3 ロック順序プロトコル（厳守）

入札・価格上昇・指値発動など `bid_participants` / `bid_events` を触るトランザクションは
必ず **`items` 行ロック → `bid_participants` → `bid_events` の順** で取得する。
逆順をやると FK ロック競合でデッドロックする（過去事故あり）。
詳細は memory: `project_bid_lock_order.md`。

---

## 3. オークションのライフサイクル

### 3.1 状態遷移

```
draft ──[出品締切]──▶ registered (各item)
                              │
auctions.status:               │
draft ──▶ scheduled ──[start命令]──▶ preparing ──[10秒pre-start]──▶ live ──[finish]──▶ finished
                                                                   │
                                                                   └──[pause]──▶ (paused: lane別)
```

### 3.2 フロアスタート（管理者操作）

**エントリポイント**: `POST /api/admin/auctions/{id}/start`
**コード**: [LiveController::start](../../../app/Http/Controllers/Admin/LiveController.php) (L233-309)

```
1. items を auction にラウンドロビン割当（プレミアム優先）
2. lanes を必要数作成（lane_count から）
3. auctions.status = 'live' / lanes.status = 'active' に更新
4. cache: auction:{id}:start_at = now+10秒、auction:{id}:lanes_to_start = [laneIds]
5. broadcast(AuctionStatusChanged('starting', countdown=10))
6. ProcessAuctionCountdownJob::dispatch($auctionId)  ← queue:countdown へ
```

### 3.3 ProcessAuctionCountdownJob のメインループ

**ファイル**: [ProcessAuctionCountdownJob.php](../../../app/Jobs/ProcessAuctionCountdownJob.php)

| パラメータ | 値 | 意味 |
|---|---|---|
| `$tries` | 1 | 失敗時に再キューしない（generation で別途復旧） |
| `$timeout` | 14400 (4時間) | ジョブ最大実行時間 |
| `TICK_INTERVAL` | 0.5秒 | tick の間隔 |
| `HEARTBEAT_TTL` | 14400秒 | heartbeat キャッシュTTL |
| `maxIterations` | 28800 (4h ÷ 0.5s) | tick 上限 |
| キュー | `countdown` | 専用ワーカー1本で逐次処理 |

**フロー**:
1. **世代チェック**（L87-90）: dispatch 後に generation が進んでいたら即終了
2. **排他ロック**（L92-100）: `countdown_job_lock:auction:{id}` を `Cache::add` で取得（既存PIDがあれば skip）
3. **10秒プレスタート**（L114-189）: `start_at` cache を見て毎秒 `AuctionStatusChanged('starting')` を broadcast → 0秒で各レーンの `startCountdown()` 呼び出し → `AuctionStatusChanged('live')` broadcast → 開始通知メール/LINE
4. **メインループ**（L196-317）: 0.5秒ごと
   - 世代チェック → 古ければ break
   - auction 状態確認（live でなければ break）
   - 全 lane を foreach
     - paused → スキップカウント
     - active → `getCountdownState()` → なければ復旧で再 `startCountdown()` → `tick(laneId)`
   - active=0 かつ paused=0 なら break
   - 10 tick (5秒) ごとに heartbeat 更新
5. **終了処理**（L319-331）: 自世代なら `countdown_job_finished:auction:{id}` を120秒セット、各種 cache を forget

### 3.4 CountdownService::tick の Phase 遷移

**ファイル**: [CountdownService.php](../../../app/Services/CountdownService.php)

```
[新商品 active]
    ▼ startCountdown(lane)  L56
phase=bidding, remaining=bid_countdown_seconds
    │
    │ ※ pre_bid をスキップする経路（startCountdown 直行）。
    │   通常は moveToNextItem→startPreBidCountdown 経由で pre_bid から入る
    │
    ▼ tick (-0.5s) L225
[remaining > 0] → broadcast(CountdownTick)
[remaining = 0] →
   activeCount >= 2 → handlePriceIncrement L431
                          ├─ items lockForUpdate
                          ├─ price 再検証（並行呼出冪等化）
                          ├─ activeCount 再カウント
                          ├─ current_price += increment, PriceEvent 記録
                          ├─ 指値保護ロジック（後述）で他者を deactivate
                          ├─ startFreezeCountdown（commit前にcache書き込み）
                          ├─ commit
                          ├─ broadcast(PriceUpdated)
                          ├─ broadcast(BidderUpdated::left) ※自動離脱発生時
                          ├─ checkBidLimits（指値発動チェック）
                          └─ remainingLimits>=2 なら adjustPriceByBidLimits

   activeCount <= 1 → handleCountdownEnd L705
                          ├─ stopCountdown（cache forget）
                          ├─ DB::tx [activeCount=0 ? unsold : finalize(sold)]
                          ├─ moveToNextItem（tx外でcache書き込み）
                          │     ├─ 次item を live に
                          │     ├─ lane.current_item_id 更新
                          │     ├─ startPreBidCountdown（phase=pre_bid）
                          │     ├─ activatePendingBidLimits（自動入札）
                          │     └─ activated>=2 なら adjustPriceByBidLimits
                          └─ broadcast(LaneItemChanged)

[freeze phase の tick] L374
   - remaining -= 0.5
   - broadcast(CountdownTick phase=freeze)
   - remaining<=0 → phase=bidding に戻して broadcast(CountdownTick phase=bidding)

[pre_bid phase の tick] L319
   - 同上 → 0秒で phase=bidding に遷移
```

### 3.5 価格上昇時の指値保護ロジック（`handlePriceIncrement` L475-527）

```
価格上昇後 (newPrice 確定後):
  1. 全 active 入札者を取得
  2. 有効な指値（is_triggered=false, limit_price>newPrice）を limit_price DESC, created_at ASC で取得
  3. 指値ホルダー = protectedUserIds とする
  4. 各 active 入札者をループ:
     - protectedUserIds に含まれる → 残す
     - 指値ホルダーがいない & lastBidder と一致 → 残す（1回限定）
     - それ以外 → deactivate（自動離脱）
  5. 落札権利者(effectiveHolder) = protected あれば最高指値ユーザー、なければ lastBidder
```

**重要**: freeze cache の書き込みは **DB::commit() より前**（L536）。理由は同ファイルコメント参照
（行ロック解放とキャッシュ反映の間の数 ms ウィンドウで誤入札を許さない）。

---

## 4. ライブ中の HTTP API 一覧

`/api/participant/auctions/{id}/live` 等は polling されないので除外。
**ライブ中に頻発する API のみ列挙**。

### 4.1 参加者向け（`auth:sanctum` + `check.role:participant`）

| Method | URL | コントローラ | 課金チェック | 副作用 | 想定頻度 |
|---|---|---|---|---|---|
| POST | `/api/participant/bids` | BidController::toggle | `check.subscription:bid` | items行ロック / cache / broadcast | 1人あたり1商品で数回〜数十回 |
| GET | `/api/participant/bids/my-active` | BidController::myActive | なし | DB read のみ | 画面更新時 |
| POST | `/api/participant/bid-limits` | BidLimitController::store | `check.subscription:bid` | DB write / cache 無 | レア |
| DELETE | `/api/participant/bid-limits/{itemId}` | BidLimitController::destroy | `check.subscription:bid` | DB write | レア |
| GET | `/api/participant/bid-limits` | BidLimitController::index | なし | DB read | 画面初期化時 |
| GET | `/api/participant/bid-limits/{itemId}` | BidLimitController::show | なし | DB read | モーダル表示時 |
| POST | `/api/participant/favorites/toggle` | FavoriteController::toggle | なし | DB write | 1人数回 |
| POST | `/api/participant/favorites/check` | FavoriteController::checkBulk | なし | DB read 一括 | 画面初期化時 |
| GET | `/api/participant/favorites` | FavoriteController::index | なし | DB read with関連 | 画面初期化時 |
| GET | `/api/participant/auctions/{id}/live` | AuctionController::live | なし | DB read（複合クエリ） | WS切断時のフォールバック |
| GET | `/api/participant/auctions/{id}/items` | AuctionController::items | なし | DB read（lane別構造化） | 画面遷移時 |
| GET | `/api/participant/items/search` | （routes inline） | なし | DB read paginate | 検索時 |
| GET | `/api/participant/recommendations` | （routes inline） | なし | DB read | 画面初期化時 |

### 4.2 管理者向け（`auth:sanctum` + `check.role:admin`）

| Method | URL | アクション | 副作用 |
|---|---|---|---|
| POST | `/api/admin/auctions/{id}/start` | LiveController::start | 全lane作成・dispatch ProcessAuctionCountdownJob |
| POST | `/api/admin/auctions/{id}/pause` | LiveController::pause | lane.status='paused', broadcast |
| POST | `/api/admin/auctions/{id}/resume` | LiveController::resume | lane.status='active', broadcast |
| POST | `/api/admin/auctions/{id}/finish` | LiveController::finish | auctions.status='finished' |
| POST | `/api/admin/lanes/{id}/next-item` | LiveController::nextItem | 強制次商品（管理者操作） |
| POST | `/api/admin/items/{id}/adjust-price` | LiveController::adjustPrice | 価格手動修正 + ログ |
| GET | `/api/admin/auctions/{id}/live` | LiveController::show | レーン別状態取得 |
| GET | `/api/admin/auctions/{id}/countdown-status` | LiveController::countdownStatus | カウントダウン状態取得 |

### 4.3 レート制限

`AppServiceProvider::boot` 内で `RateLimiter::for(...)` を 4 種類定義し、`api.php` 側で
`throttle:<name>` ミドルウェアを書込み系エンドポイントに付与している。

| limiter 名 | 制限 | 適用エンドポイント | キー |
|---|---|---|---|
| `bids` | 4 req/sec | `POST /api/participant/bids` | user.id ?? ip |
| `bid-limits` | 2 req/sec | `POST /api/participant/bid-limits`, `DELETE /api/participant/bid-limits/{itemId}` | user.id ?? ip |
| `favorites` | 60 req/min | `POST /api/participant/favorites/toggle`, `POST /api/participant/favorites/check` | user.id ?? ip |
| `participant-general` | 300 req/min | （現在は読取系には未適用、将来予約） | user.id ?? ip |

超過時は `429 Too Many Requests` を返す（`bootstrap/app.php` の例外ハンドラで日本語メッセージ化）。
`Cache::lock("bid_inflight:item:X", 5)->block(0.3)` と組み合わせて、通常リトライは
クライアント実装が無くても自然に成功するよう調整済み（[JoinBidAction.php:76-86](../../../app/Actions/Bid/JoinBidAction.php#L76-L86)）。

---

## 5. WebSocket（Reverb）イベント

### 5.1 接続情報

| 項目 | 値 |
|---|---|
| ドライバ | Reverb |
| サーバ | `0.0.0.0:6001`（`config/reverb.php`） |
| クライアント | Pusher プロトコル互換（Laravel Echo） |
| プロセス管理 | Supervisor（`/etc/supervisor/conf.d/reverb.conf`） |

> **❗ 注意（memory: project_reverb_port_pitfall.md）**
> nginx の port 8080 は WebSocket **専用 proxy**。
> `BROADCAST_HOST` を 8080 に向けるとサーバ→Reverb 接続が Connection reset し、
> 全件 unsold 化する事故が過去発生。`.env` は **6001 を直接** 指定する。

### 5.2 全イベント定義

すべて `auction.{auctionId}.live` チャンネルへ broadcast。

| クラス | broadcastAs | データ | 発火タイミング |
|---|---|---|---|
| `AuctionStatusChanged` | `auction.status` | status, message, countdown_seconds | スタート / pre-start カウント / 終了 |
| `CountdownTick` | `countdown.tick` | lane_id, item_id, remaining, active_count, price, phase | **0.5秒ごと（高頻度）** |
| `BidderUpdated` | `bidder.updated` | item_id, lane_id, active_count, event_type(`joined`/`left`) | 入札ON/OFF / 自動離脱 |
| `PriceUpdated` | `price.updated` | price, active_count, bid_seconds, auto_left_user_ids | 価格上昇時 |
| `LaneItemChanged` | `lane.changed` | lane_id, previous_item, current_item | 商品切替 |
| `ItemSold` | `item.sold` | item_id, winner_id, final_price | 成約確定 |
| `BidLimitReached` | `bid.limit.reached` | item_id, user_id, current_price, limit_price | 指値発動 |

### 5.3 0.5秒間隔 broadcast の負荷見積り

- N レーンのオークション → CountdownTick が `2 × N` 回/秒
- 12 レーンなら 24 イベント/秒、各イベントは数百 byte
- 接続クライアント数 × 24 = Reverb の outbound 帯域
- 300名 × 24msg/s ≈ 7,200 msg/s（数MB/sのオーダー）→ ALB / Reverb スケーリング検討必要

詳細はリスク評価ドキュメント §3。

### 5.4 フロント購読側

[useAuctionSocket.ts](../../../resources/ts/hooks/useAuctionSocket.ts)

```ts
const channel = echo.channel(`auction.${auctionId}.live`);
channel.listen('.countdown.tick', onCountdownTick);
channel.listen('.price.updated', onPriceUpdated);
channel.listen('.bidder.updated', onBidderUpdated);
channel.listen('.lane.changed', onLaneChanged);
channel.listen('.item.sold', onItemSold);
channel.listen('.auction.status', onAuctionStatus);
channel.listen('.bid.limit.reached', onBidLimitReached);
```

各 callback で React state 更新 / TanStack Query の `invalidateQueries` を呼ぶ。

---

## 6. キュー / Redis 構成

### 6.1 ジョブとキューのマッピング

| Job | onQueue | 1オークションあたり | 走るタイミング |
|---|---|---|---|
| `ProcessAuctionCountdownJob` | **`countdown`** | 1本のみ（auction単位） | フロアスタート時 / heartbeat 死亡時に MonitorAuctionJobs から再 dispatch |
| `ProcessCountdownJob`（旧型） | `countdown` | レーン単位（廃止予定） | 現在は使用していないが互換のため残存 |
| `SendAuctionPreviewJob` | デフォルト | 全 auction 1本 | 毎日 18:00（前日の予告通知） |
| `SendPaymentReminderJob` | デフォルト | バッチ | 30分毎 |
| `SendLineNotificationJob` | `notifications` | 都度 | LINE通知の都度 dispatch |
| `ProcessItemVideoJob` | デフォルト | 商品ごと | 動画アップロード時 |

### 6.2 Supervisor 構成（本番）

```
/etc/supervisor/conf.d/
├── queue-default.conf       … 汎用ジョブ（メール・通知等）
├── queue-countdown.conf     … カウントダウン専用 ★ ProcessAuctionCountdownJob はここで処理
├── queue-notifications.conf … 通知専用
└── reverb.conf              … WebSocket サーバ
```

**❗ countdown キューワーカーは1プロセスで十分**（同時オークション数 = ジョブ数）だが、
`numprocs` を増やすと複数オークション並走時に有利。
`Cache::add` の lock があるので二重実行はしない設計。

### 6.3 Redis キー（cache）

| キー | TTL | 役割 |
|---|---|---|
| `countdown:lane:{laneId}` | 14400s (`CountdownService::CACHE_TTL`) | レーンのカウントダウン状態 (phase, remaining, last_bidder_user_id 等) |
| `countdown_job_generation:auction:{id}` | 14400s | ジョブの世代番号（再 dispatch 時に +1） |
| `countdown_job_running:auction:{id}` | timeout+600s | 実行中フラグ |
| `countdown_job_lock:auction:{id}` | timeout+60s | PID 入りの排他ロック（`Cache::add`） |
| `countdown_job_heartbeat:auction:{id}` | 14400s | 最終 tick の Unix ts（5秒ごと更新） |
| `countdown_job_finished:auction:{id}` | 120s | 正常終了マーカー（Monitor が再 dispatch しないように） |
| `auction:{id}:start_at` | プレスタート期間 | プレスタートのターゲット時刻 |
| `auction:{id}:lanes_to_start` | プレスタート期間 | プレスタート明けに startCountdown する lane id 配列 |
| `bid_inflight:item:{itemId}` | 5s | 同一商品の入札直列化用ロック（Redis Cache::lock） |

### 6.4 retry_after について

`config/queue.php` で `retry_after = 14400` 秒（4時間）。
**理由**: countdown ジョブは最長 4 時間動く想定。デフォルト 90 秒だと「処理中だがまだ ack していない」と判定され、二重実行される。

ただしこれは **MonitorAuctionJobs（heartbeat 監視）** と独立した安全機構。
heartbeat が30秒以上途絶 → MonitorAuctionJobs が generation+1 して再 dispatch → 古いジョブは次の tick で `isCurrentGeneration()=false` を検出して自発終了。

---

## 7. スケジュールタスク（cron / schedule）

[bootstrap/app.php](../../../bootstrap/app.php) L56-91

| Schedule | コマンド | 役割 |
|---|---|---|
| 毎分 | `announcements:publish-scheduled` | お知らせ公開 |
| 毎分 | `auctions:start-scheduled` | scheduled_start_at 到達した auction を自動 start |
| **毎分** | **`auctions:monitor-jobs`** | **★ ライブ中の countdown ジョブが死んでいないか監視** |
| 30分毎 | `SendPaymentReminderJob` | 未入金リマインダー |
| 18:00 daily | `SendAuctionPreviewJob` | 翌日開催の予告通知 |
| Mon 09:00 | `reports:generate weekly` | 週次レポート |
| 1日 09:00 | `reports:generate monthly` | 月次レポート |
| 03:00 daily | `subscriptions:renew` | サブスク更新 |

### 7.1 MonitorAuctionJobs（重要）

**ファイル**: [MonitorAuctionJobs.php](../../../app/Console/Commands/MonitorAuctionJobs.php)

ライブ中の auction を全件取得し、各 auction について:

1. `countdown_job_finished:auction:{id}` キャッシュがあれば skip（直前に正常終了したジョブ）
2. `countdown_job_heartbeat:auction:{id}` を取得 → `now - heartbeat < 30s` なら生存中とみなして skip
3. それ以外（heartbeat なし or 30秒超過）+ active/paused lane が存在 → 死亡判定:
   - 全 cache を forget
   - generation を +1
   - `ProcessAuctionCountdownJob::dispatch($auctionId)` で新ジョブを起動

**`HEARTBEAT_STALE_SECONDS = 30`** （L23）: 30秒間 heartbeat が来なかったら死亡判定。

> **❗ ハートビートTTLとMonitor閾値の関係**
> heartbeat TTL = 14400 秒（4時間）。30秒で stale 判定するので、TTL 切れより前に Monitor が動く。
> 監視周期は毎分なので、最悪 60s 以内に検知 → 復旧 dispatch。

---

## 8. フロー詳細：典型シナリオ

### 8.1 「100名が同一商品にアグレッシブ入札」のミリ秒挙動

前提: 商品X、開始価格1000円、現在 active=0、phase=bidding

```
T+000ms  100人が一斉に POST /bids (is_active=true)
T+000ms  100リクエストが nginx → php-fpm に到達
T+010ms  100リクエストが Cache::lock("bid_inflight:item:X", 5) を取得試行
T+010ms  最初の1人 (User A) が lock 取得成功、残り99人は失敗 → 即座に
         "現在他のユーザーの入札を処理中です。少しお待ちください" レスポンス
T+020ms  User A: items.lockForUpdate（DB） → phase=bidding 確認 → price=1000確認
T+040ms  User A: bid_participants.participate → bid_events.recordJoin
T+050ms  User A: activeCount=1 → broadcast(BidderUpdated::joined) ※toOthers
T+060ms  User A: activeCount<2 なので handleImmediatePriceIncrement は呼ばない
T+070ms  User A: bid_inflight lock release
T+070ms  User A: success レスポンス返却

T+100ms  失敗した99人がリトライ（フロントの自動再送 or 手動）
         → 続けて1人ずつ inflight lock を取りながら順次処理
T+1〜10s  99人が順番に join → 各 join で activeCount が 2,3,...と増加
         → 2人目の join 時点で handleImmediatePriceIncrement が走る
         → User A 以外の active を全 deactivate（保護対象いなければ
           lastBidder = 2人目の入札者を残す）
         → freeze countdown 開始 → broadcast(PriceUpdated) → 価格1100円に
         → User A も自動離脱（最後の入札者じゃない）
```

**この挙動は意図された設計**:
- Redis lock のおかげで 100 リクエストが items 行ロックの DB キューに並ばない
- 1人1人が逐次処理されるので、freeze cache が反映される前に次の人が join するチェーンが起きない

### 8.2 freeze タイミングの詳細

**目的**: 価格上昇直後の「見間違い & 連打」誤入札を防ぐ。

```
T=0     phase=bidding, remaining=2.0s
T=2.0   tick → remaining=0.0 → activeCount=2 で handlePriceIncrement
        ├─ DB tx: items行ロック → 価格上昇 → bid_participants 整理
        ├─ startFreezeCountdown ← cache に phase='freeze' を ★commit前★ に書く
        ├─ DB commit
        └─ broadcast(PriceUpdated)
T=2.0+  tick で phase='freeze', remaining=2.0s（freeze_countdown_seconds）
T=2.5   tick (-0.5) → broadcast(CountdownTick phase=freeze, 1.5s)
T=3.0   tick (-0.5) → broadcast(CountdownTick phase=freeze, 1.0s)
T=3.5   tick (-0.5) → broadcast(CountdownTick phase=freeze, 0.5s)
T=4.0   tick (-0.5) → remaining=0 → phase=bidding に戻す → broadcast(initial bidding tick)
T=4.0+  通常の bidding カウントダウン
```

**フロントエンド側の挙動**:
- `phase=freeze` の間は入札ボタンを `disabled` にする
- API 側でも JoinBidAction L51-56 で freeze 中の入札は弾く（二重防御）

### 8.3 ❗ pre_bid フェーズ中の自動入札（指値）バイパス

**現状の問題的挙動** ([ProcessAuctionCountdownJob.php:151-161](../../../app/Jobs/ProcessAuctionCountdownJob.php#L151-L161))

最初の商品にも事前指値の自動入札を適用するため、`activatePendingBidLimits` が
`SetBidLimitAction` 経由で **JoinBidAction を経由せずに** `BidParticipant::participate()` を直接呼ぶ。

→ pre_bid phase 中の bid 拒否ロジックを通らない（自動入札なので OK だが、設計として別経路）。
→ `bid_inflight:item` lock も取らない。
→ これ自体は意図された設計（pre_bid 中に指値だけ事前 active 化したい）だが、
  以下の場合は注意:
  - 同じ瞬間にユーザーが手動 ON タップ → JoinBidAction が pre_bid で拒否される（OK）
  - adjustPriceByBidLimits が走るのは pre_bid 終了後ではなく activate 直後 → tick が pre_bid を消化中に price が上がる挙動になる

リスク評価ドキュメント §4 で再掲。

---

## 9. クライアント側実装

### 9.1 参加者ライブ画面

| ファイル | 役割 |
|---|---|
| [resources/ts/pages/participant/AuctionLive.tsx](../../../resources/ts/pages/participant/AuctionLive.tsx) | レーン別ライブ画面、useAuctionSocket で WS 購読 |
| [resources/ts/pages/participant/AuctionItems.tsx](../../../resources/ts/pages/participant/AuctionItems.tsx) | 商品一覧（事前指値設定可能） |
| [resources/ts/hooks/useAuctionSocket.ts](../../../resources/ts/hooks/useAuctionSocket.ts) | Reverb 購読フック |
| [resources/ts/hooks/useBidToggle.ts](../../../resources/ts/hooks/useBidToggle.ts) | 入札ON/OFF mutation |
| [resources/ts/components/BidButton.tsx](../../../resources/ts/components/BidButton.tsx) | 入札ボタン UI（freeze時 disabled） |
| [resources/ts/components/CountdownDisplay.tsx](../../../resources/ts/components/CountdownDisplay.tsx) | 残り秒数表示 |

### 9.2 管理者ライブ画面

| ファイル | 役割 |
|---|---|
| [resources/ts/pages/admin/LiveControl.tsx](../../../resources/ts/pages/admin/LiveControl.tsx) | スタート/一時停止/次商品/手動価格調整 UI |
| [resources/ts/hooks/useLiveControl.ts](../../../resources/ts/hooks/useLiveControl.ts) | 管理者向け状態取得 + WS購読 |

### 9.3 状態同期戦略

- **TanStack Query** + **Reverb listen** のハイブリッド
- WS イベント受信時 → `queryClient.invalidateQueries` で該当キーを再フェッチ
- staleTime: 商品リストは 30 秒
- WS 切断時はポーリングフォールバック（2秒間隔）あり
- 接続復帰時は `/auctions/{id}/live` を即フェッチして整合回復

---

## 10. ロギング / モニタリング

### 10.1 アプリケーションログ

```
storage/logs/laravel.log    全般
storage/logs/audit.log      入札・落札の監査
/var/log/supervisor/queue-countdown.log   countdown ワーカー出力
/var/log/supervisor/reverb.log            WebSocket
```

### 10.2 構造化ログ箇所（重要）

- `Countdown started: lane {id}, item {id}, bid={s}s, freeze={s}s`
- `Pre-start countdown for auction {id}`
- `Auto-left users on price increment: item={id}, left=...`
- `BidLimit triggered & cancelled: item={id}, user={id}, ...`
- `MonitorAuctionJobs: Re-dispatched countdown job for auction {id}`

### 10.3 CloudWatch メトリクス（`MetricRecorder`）

`app/Services/Monitoring/MetricRecorder.php` で AWS CloudWatch にカスタム値を送信:

- `bidFailure` … 入札失敗（理由付き）
- `priceIncrementFailed` … 価格上昇エラー
- `broadcastFailure` … broadcast 例外
- `countdownTick` … tick の各 phase 計上
- `priceIncrement` … 成功した価格上昇
- `jobFailure` … ジョブ失敗

> **memory: project_countdown_log_permission_fragility.md**
> ジョブ冒頭の `Log::info()` がログ書き込み権限失敗で例外を吐くと、tick チェーン全体が停止し
> 「3秒のまま」現象が発生する古典的事故あり。
> ログディレクトリの権限と DiskSpace（df）は本番開始前に必須チェック。

---

## 11. 既知の挙動と仕様メモ

### 11.1 throttle 設定済み

`AppServiceProvider::boot` で `RateLimiter::for('bids' / 'bid-limits' / 'favorites' / 'participant-general')`
を定義し、`api.php` 側で `throttle:<name>` を書込み系に付与済み。詳細は §4.3。

### 11.2 cache TTL = 14400s（4時間）

`countdown:lane:{id}` の cache は **4 時間** で expire する設計（`CountdownService::CACHE_TTL`）。
ジョブの heartbeat TTL と一致させているので、長時間ポーズ中に cache だけ消えて
freeze スキップ等の整合崩れが起きないようにしている。
正常稼働中は tick 時に毎回 `Cache::put(..., self::CACHE_TTL)` で延長される。

### 11.3 retry_after ≠ tries=1

`ProcessAuctionCountdownJob::tries = 1` なので失敗してもキュー再投入はない。
heartbeat 監視 + generation で復旧する独自プロトコル。

### 11.4 BidParticipant の物理一意性

`UNIQUE(item_id, user_id)` なので、ON→OFF→ON も同じ row を `is_active` トグルする。
`BidParticipant::participate()` は upsert 実装。
履歴は `bid_events` 側に追記される。

### 11.5 finalize（成約確定）の経路

成約確定は **`handleCountdownEnd` 内のみ**。
`activeCount == 1` で `FinalizeBidAction` が走り、
`won_items` に レコード作成 → `items.status='sold'` → ItemSold broadcast。
管理者が `LiveController::nextItem` で強制 next すると、
内部で `MoveToNextItemAction` が同等処理を呼ぶ。

---

## 12. 改修・運用時の注意

1. **マイグレーション中はライブ禁止** — items / bid_participants / lanes をオンライン変更すると行ロック衝突。
2. **php-fpm 再起動はライブ中禁止** — 進行中の入札 tx が ROLLBACK して状態が乱れる。
3. **supervisor reload も基本ライブ中禁止** — countdown ワーカーが kill されると heartbeat 死 → MonitorAuctionJobs が再起動するが、tick が数秒スキップされる。やむを得ない場合は `supervisorctl restart queue-countdown` のみで他は触らない。
4. **Reverb 再起動は致命的** — 全クライアントが再接続するまで broadcast がロスト。再接続中に成約が走ると、その商品の落札通知をクライアントが受信できない（API 取得時には反映済）。
5. **`.env` 変更後は必ず `config:cache`** — `BROADCAST_HOST` などのキャッシュが古いと WS 経路がずれる。

---

## 13. 関連ドキュメント

- 運用者向けフロー解説: [ライブオークション運用ガイド.md](./ライブオークション運用ガイド.md)
- 300名負荷リスク評価＆ロジックレビュー: [300名負荷リスク評価とレビュー指摘.md](./300名負荷リスク評価とレビュー指摘.md)
- 第一回開催前緊急対応: [docs/operations/2026-04-29_第一回オークション開催前緊急対応仕様書.md](../../operations/2026-04-29_第一回オークション開催前緊急対応仕様書.md)
- インシデント対応フロー: [docs/operations/インシデント対応フロー.md](../../operations/インシデント対応フロー.md)
- 保守マニュアル: [docs/operations/保守マニュアル.md](../../operations/保守マニュアル.md)
