# GMOあおぞらネット銀行 API連携 方針書（記録）

- 作成日: 2026-06-17
- ステータス: **未実装 / ヒアリングシート提出待ち**（後日あらためて実装着手予定）
- 目的: 落札の入金消込を自動化する。GMOあおぞらの「振込入金口座 入金明細通知」をWebhookで受け、auction側で入金検知 → 落札の支払い消込へ繋ぐ。

---

## 1. ヒアリングシート 確定回答

| # | 設問 | 確定回答 | 備考 |
|---|------|----------|------|
| Q1 | 連鎖接続先 | なし | auctionが直接GMO APIを叩く |
| Q2 | 認可方式 | OAuth 2.0 | 自社法人口座の入金明細取得が目的。OIDCは不要 |
| Q3 | クライアント認証 | client_secret_basic | secretをAuthorizationヘッダで送信 |
| Q4 | 本番 固定IP | **■要記入** 本番EC2のEIP（`curl ifconfig.me`で確認） | EC2直接EIP構成 |
| Q5 | 本番 ドメイン | **■要記入** 本番ドメイン | 未登録ならIPのみ可 |
| Q6 | 開発 固定IP | **■要記入** medaka-auction.com(ステージング)EC2のEIP | Q4と別IP必須 |
| Q7 | 開発 ドメイン | medaka-auction.com | 旧本番→開発/ステージングに転用済 |
| Q8 | 災対環境 | なし | Q9該当なし |
| Q10 | 本番リダイレクト | `https://<本番ドメイン>/api/gmo-aozora/oauth/callback` | ドメイン確定後に確定 |
| Q11 | 開発リダイレクト | `https://medaka-auction.com/api/gmo-aozora/oauth/callback` | |
| Q12 | 災対リダイレクト | 該当なし | |
| Q13 | テスト用固定IP | **■要記入** 作業端末/踏み台のグローバルIP | Q4と別IP必須 |
| Q14 | Webhook利用 | 利用する | |
| Q15 | 通知先URL 本番 | `https://<本番ドメイン>/api/gmo-aozora/webhook` | |
| Q16 | 通知先URL 開発 | `https://medaka-auction.com/api/gmo-aozora/webhook` | |
| Q17 | メッセージ種別 | 振込入金口座 入金明細通知 | |
| Q18 | セキュリティ | 両方（Signature + Authorization/Basic） | Webhook受信を二重検証 |
| Q23 | API利用開始希望日 | **■要記入** 事業側で指定 | |
| Q24 | 開発環境希望日 | **■要記入** 事業側で指定 | |

### Webhook Basic認証値（2026-06-17 生成 / `.env`管理）

> ⚠️ これらはチャット履歴に残った値。本番稼働時はローテーション推奨。GMOフォームで記号(`/ +`)が弾かれる場合は英数字限定で再生成すること。

```
Q19 Basic 本番 user : gmo-wh-prod-03c0e8b8
Q20 Basic 本番 pass : RHsZcaNpRQYeXFYmscS8gEf8GMdWK9HG
Q21 Basic 開発 user : gmo-wh-dev-02a94253
Q22 Basic 開発 pass : qG6/dCKSaeRmIBOxbQhdIUMyMK0nZQkW
```

`.env` 例:
```
GMO_WEBHOOK_BASIC_USER=gmo-wh-prod-03c0e8b8
GMO_WEBHOOK_BASIC_PASS=RHsZcaNpRQYeXFYmscS8gEf8GMdWK9HG
```

---

## 2. 提出前に埋める残り実値（`■`）

1. 本番EC2のEIP（Q4） … 本番箱で `curl -s ifconfig.me`
2. 本番ドメイン（Q5） … 未登録ならIPのみで可
3. テスト用グローバルIP（Q13） … Q4と別であること
4. API利用開始希望日 / 開発環境希望日（Q23/Q24） … 事業側スケジュール

※ EIPと本番ドメインが確定すれば Q10・Q15 の `<本番ドメイン>` も自動確定。

---

## 3. 後日の実装スコープ（着手時にここから）

現状コードにGMO連携は存在しない（`grep -riE "gmo|aozora"` ヒットなし）。提出後、以下を実装する。

1. **ルート**: `POST /api/gmo-aozora/webhook`、`GET /api/gmo-aozora/oauth/callback`
2. **OAuthトークン管理**: client_secret_basicでアクセストークン取得・リフレッシュ（自社口座を一度認可）。トークンは安全に永続化
3. **Webhook受信の検証（二重）**:
   - Basic認証: `hash_equals()` で `.env` の user/pass と照合 → 不一致は401（middleware推奨）
   - Signature検証: GMO付与の署名をシークレットで検証
4. **入金消込**: ペイロード（振込入金口座の入金明細）を受け、落札の入金に突合 → 支払い済み更新。支払期限「翌水曜23:59」運用（[[project_payment_deadline_policy]]）と接続
5. **冪等性**: 同一明細の再通知に備え、明細ID等でユニーク制御
6. **ネットワーク**: GMOへのアウトバウンドは本番EC2のEIP固定（IP申告済みのため経路変更時は再申告要）

---

## 4. 再開時の合図

「GMO連携の実装お願いします」で本書の §3 から着手する。提出済みシートの実値（EIP/ドメイン/希望日）が確定していれば併せて本書 §1・§2 を更新すること。
